#include<stdio.h>
int main() {
    char ch;
    printf("Enter the alphabet:");
    scanf("%c", &ch);
    if(ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'U')
    {
        printf("%c is the vowel", ch);
    }
    else {
        printf("%c is the consonent", ch);
    }
    return 0;
}
